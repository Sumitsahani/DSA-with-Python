# Description
# You are given a string stored in a variable with the name string
# The length of the string is stored in a variable with the name N
# You have to print the number of substrings the string string contains which start vowels a, e, i, ooru.
# For example, we have a string string abc and N 3. Therefore substrings formed are
# a
# ab
# abc
# b
# bc
# C
# -The substrings which start with vowels are a, ab and abc. Therefore the required output is 3
# Input
# The first line of the input contains the value stored in N
# The second line contains the value stored in string
# Output
# Print the number of substrings the string str contains which start vowels a, e, i, o oru.

